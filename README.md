# junk-drawer

всіляка дрібна хрінь, яку шкода загубити.

кожна річ — у своїй підпапці.

## Ліцензія

Код тут — MIT, див. `LICENSE`. Він не поширюється на чуже, що лежить поруч:

* `expressvpn/` — CC0 1.0, власний `LICENSE` у самій теці;
* `grub-sddm-hidpi/theme/*.pf2` — зібрані `grub-mkfont` із DejaVu Sans Mono,
  чинна ліцензія DejaVu Fonts;
* `grub-sddm-hidpi/theme/background.jpg` і `grub-sddm-hidpi/assets/nebula-noir.jpeg` —
  шпалери KDE, права авторів оригіналу; лежать тут лише щоб скрипт відтворив
  той самий вигляд;
* `cnc/**/*.nc` — програми, зняті з верстатів. Не мої, наведені як зразки
  даних, правами на них не розпоряджаюся.
